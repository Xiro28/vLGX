context: null

strings: null
    
extract:

- step(number): 
    prompt: |
        Extract the steps (specified as a number) shown inside the page.
    knowledge_base: |
        #show step/1.

- main_action(step, action):
    prompt: |
        Extract the main action (which includes the parts with a label) that has to done.
    knowledge_base: |
        #show main_action/2.

- step_description(step, description): 
    prompt: |
        Describe shortly the action being performed as an enumered list of action. 
        The description must include also the main action.
    extraction_condition:
        - condition: step(_)
          monotone: true
    knowledge_base: |
        #show step_description/2.


knowledge_base: null
